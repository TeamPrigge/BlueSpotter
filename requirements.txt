# BlueSpotter — phase 0 dependencies
# Install in Colab with:  pip install -r requirements.txt
# (Colab already ships torch with CUDA; cellpose will use the existing GPU build.)

cellpose>=4.0            # Cellpose-SAM
mlflow>=2.14
pyyaml>=6.0
numpy>=1.26,<2.1            # numba/cellpose need numpy<2.1 (Colab ships 2.x)
tifffile>=2024.1        # read/write TIFF image + mask stacks
scikit-image>=0.22      # morphology / feature extraction (downstream)
matplotlib>=3.8         # quick visual QC

# Data versioning. Plain `dvc` is enough: the remote is a directory on the mounted
# Drive, so none of the cloud extras (dvc[gdrive], dvc[s3], ...) are needed.
dvc>=3.50

# Dev / repo hygiene (optional locally; not needed for a training run)
nbstripout>=0.7
pre-commit>=3.7
pytest>=8.0
ruff>=0.6
